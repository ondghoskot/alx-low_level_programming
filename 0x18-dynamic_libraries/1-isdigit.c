#include "main.h"

/**
 * _isdigit - prints 1 if input is a digit
 * @c: input that represents the digit
 * Return: 1 if true 0 otherwise
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
