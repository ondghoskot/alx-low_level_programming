#include <stdio.h>

/**
 * main - prints the name of the file using argv
 * @argc: number of CLA
 * @argv: array of strings containing CLA
 * Return: 0 (success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
