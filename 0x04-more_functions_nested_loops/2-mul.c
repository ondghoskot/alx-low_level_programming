#include "main.h"

/**
 * mul - takes in 2 integers and multiplies them together
 * @a: first input integer
 * @b: second input integer
 * Return: value of multiplication
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
