#include "main.h"

/**
 * _isupper - prints 1 if c is uppercase
 *
 * @c: random character
 *
 * Return: 1 or 0 if c is not uppercase
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
